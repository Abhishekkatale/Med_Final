// Jest setup file (optional)
// You can use this file to set up global configurations or mocks for your tests.

// For example, you might want to set a default environment variable for tests:
// process.env.JWT_SECRET = 'test-secret-key';

// Or initialize something global:
// console.log('Jest setup: Initializing test environment...');

// For now, it can be kept simple or even empty if no global setup is immediately needed.
// Ensure any modules imported here are available in the test environment.
// If using 'supertest' with an Express app, you might initialize your app instance here
// or more commonly, within each test suite that needs it.

// Example: Mocking a global function or module (if needed across many tests)
/*
jest.mock('some-module', () => ({
  someFunction: jest.fn(() => 'mocked value'),
}));
*/
console.log('Jest setup file loaded.');
